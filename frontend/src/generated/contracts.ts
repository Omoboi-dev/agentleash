// AUTO-GENERATED by scaffold-stacks. DO NOT EDIT.
// Re-run `stacksdapp generate` to update.

import { request } from '@stacks/connect';
import { fetchCallReadOnlyFunction, cvToValue, ClarityValue } from '@stacks/transactions';
import { getReadOnlyNetwork, scaffoldConfig } from '../scaffold.config';
import { callDevnetContract, getDevnetSenderAddress } from '../lib/devnet';

let _deployments: Record<string, any> = { contracts: {} };
try { _deployments = require('./deployments.json'); } catch {}

function getContractId(name: string): { address: string; contractName: string } {
  const contractId: string = _deployments?.contracts?.[name]?.contract_id ?? '';
  if (!contractId) {
    console.warn(`[scaffold-stacks] "${name}" not deployed. Run stacksdapp deploy.`);
    return { address: '', contractName: name };
  }
  const dot = contractId.lastIndexOf('.');
  return { address: contractId.slice(0, dot), contractName: contractId.slice(dot + 1) };
}


// ── leash ──────────────────────












export async function leash_createPolicy(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'create-policy',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'create-policy',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_deposit(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'deposit',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'deposit',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_pay(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'pay',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'pay',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_revoke(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'revoke',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'revoke',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_setActive(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'set-active',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'set-active',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_setLimits(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'set-limits',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'set-limits',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_setRecipient(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'set-recipient',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'set-recipient',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function leash_withdraw(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('leash');
  if (!address) return undefined;
  if (scaffoldConfig.isDevnet) {
    return callDevnetContract({
      contract: `${address}.${contractName}`,
      functionName: 'withdraw',
      functionArgs,
      postConditions,
    });
  }
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'withdraw',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}





export async function leash_getAgents(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('leash');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-agents',
    functionArgs,
    network: getReadOnlyNetwork(),
    senderAddress: senderAddress ?? getDevnetSenderAddress() ?? address,
  });
  return cvToValue(result);
}




export async function leash_getAvailable(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('leash');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-available',
    functionArgs,
    network: getReadOnlyNetwork(),
    senderAddress: senderAddress ?? getDevnetSenderAddress() ?? address,
  });
  return cvToValue(result);
}




export async function leash_getPolicy(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('leash');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-policy',
    functionArgs,
    network: getReadOnlyNetwork(),
    senderAddress: senderAddress ?? getDevnetSenderAddress() ?? address,
  });
  return cvToValue(result);
}




export async function leash_isRecipientAllowed(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('leash');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'is-recipient-allowed',
    functionArgs,
    network: getReadOnlyNetwork(),
    senderAddress: senderAddress ?? getDevnetSenderAddress() ?? address,
  });
  return cvToValue(result);
}



