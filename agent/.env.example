# Approved recipient the demo agent pays.
MERCHANT=ST...

# Optional. An address that is not approved (defaults to the contract deployer).
# STRANGER=ST...

# Optional. Where the agent key lives (defaults to ~/.stacks-keys/agentleash-agent.json).
# AGENT_KEY_FILE=~/.stacks-keys/agentleash-agent.json
